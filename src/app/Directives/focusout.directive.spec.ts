import { FocusoutOnEnterDirective } from './focusout.directive';

describe('FocusoutOnEnterDirective', () => {
  it('should create an instance', (e:any) => {
    const directive = new FocusoutOnEnterDirective(e);
    expect(directive).toBeTruthy();
  });
});
