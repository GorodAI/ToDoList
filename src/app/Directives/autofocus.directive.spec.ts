import { AutofocusDirective } from './autofocus.directive';

describe('AutofocusDirective', () => {
  it('should create an instance', (e:any) => {
    const directive = new AutofocusDirective(e);
    expect(directive).toBeTruthy();
  });
});
